export interface ProductOrderDetail{
    id: number;
    quantity: number;
    price: number;
}

export interface ProductResume{
    name: string;
    quantity: number;
    price: number;
    total: number;
}